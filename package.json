{
  "name": "wedding-page",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "canvas-confetti": "^1.9.2",
    "prettier": "^3.2.5",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-reveal": "^1.2.2",
    "react-scripts": "3.1.0"
  },
  "scripts": {
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "format": "yarn prettier ./src --write && yarn prettier ./public --write"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
